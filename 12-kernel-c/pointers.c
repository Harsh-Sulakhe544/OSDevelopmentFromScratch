#include <stdio.h>

void func() {
    char* string = "Hello";
    printf("%s\n", string); // Print the string to verify it works
}

int main() {
    func(); // Call the function
    return 0; // Return success
}
